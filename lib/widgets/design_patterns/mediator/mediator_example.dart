import 'package:flutter/widgets.dart';

class MediatorExample extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Mediator Example!'),
    );
  }
}
