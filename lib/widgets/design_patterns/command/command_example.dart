import 'package:flutter/widgets.dart';

class CommandExample extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Command Example'),
    );
  }
}
