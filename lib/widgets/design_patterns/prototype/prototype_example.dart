import 'package:flutter/widgets.dart';

class PrototypeExample extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Prototype example!'),
    );
  }
}
