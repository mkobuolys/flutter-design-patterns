import 'package:flutter/widgets.dart';

class BridgeExample extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Bridge Example!'),
    );
  }
}
