import 'package:flutter/widgets.dart';

class ProxyExample extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Proxy Example!'),
    );
  }
}