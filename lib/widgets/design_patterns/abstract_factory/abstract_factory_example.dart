import 'package:flutter/widgets.dart';

class AbstractFactoryExample extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Abstract Factory Example'),
    );
  }
}
